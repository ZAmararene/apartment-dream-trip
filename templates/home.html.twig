{% extends 'base.html.twig' %}
{% block title %}Locations de vacances{% endblock %}
{% block body %}
<div class="banner container-fluid">
    <div class="text-center">
        <h1 class="display-3 mb-4">Locations de vacances</h1>
        <p class="lead">Nous mettons en relation les voyageurs et les propriétaires qui veulent profiter d'opportunités et
            augmenter leurs revenus en proposant leurs Biens à la location.
        </p>
        <p>Faites découvrir votre logement à des millions de voyageurs</p>

        <hr class="my-4">

        <p>Publier une annonce personnalisée sur SymBNB n'a jamais été aussi simple</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg mt-3" href="{{ path('ads_index') }}" role="button">Voir nos annonces</a>
        </p>
    </div>
</div>

<div class="container">
    <h2 class="h1 my-5">Appartements stars</h2>

    <div class="row">
        {% for data in ads %}
            {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
        {% endfor %}
    </div>

    <h2 class="h1 mt-3 mb-5">Propriétaires stars</h2>
    <div class="row">
        {% for data in users%}
        <div class="col">
            <div class="card border-secondary mb-3 rounded-0">
                <div class="card-body">
                    <h5 class="card-title text-center">{{ data .user.fullName }}</h5>
                    <hr>
                    <div class="card-text">
                        <img src="{{ data.user.picture }}" alt="Avatar de {{ data.user.fullName }}" class="float-left mr-3">
                        <p class="text-justify">{{ data.user.introduction }}</p>
                        <a href="{{ path('user_show', {'slug': data.user.slug}) }}" class="btn btn-primary float-right">En savoir plus</a>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}